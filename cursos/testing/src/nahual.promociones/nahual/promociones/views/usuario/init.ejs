<h2>Usuarios</h2>
<a href='javascript://' class='add'>Agregar Nuevo Usuario</a>
<table class="list" cellspacing='0px'>
	<thead>
	<tr>
		<% for(var i = 0; i < Nahual.Promociones.Models.Usuario.editableAttributes.length; ++i){ 
			var attr = Nahual.Promociones.Models.Usuario.editableAttributes[i];
			if (attr.ShowInList) {
		%>
				<th><%= attr.Description%> </th>	 
		<%
			}
		}%>
		<th>Options</th>
	</tr>
	</thead>
	<tbody>
		<%= $.View('//nahual/promociones/views/usuario/list',{usuarios: usuarios})%>
	</tbody>
</table>
